Do sprawdzenia:

- GLTF i Clone żeby nie wczytywać ten sam obiekt parę razy
- elastic search
- czy da się żeby obiekt pojawiał się mniej więcej w tym miejscu gdzie go przesunąłem


Do zrobienia większe:

- Zdockeryzować
- ujednolicić miniaturki można napisać skrypt w blenderze który wczyta pliki bez materiałów i zrobi ich zrzut ekranu
- wczytywać elementy w odpowiednim momencie


Do zrobienia mniejsze:

- czy da się wyrównywać żeby zaczynały się na płaszczyźnie
- ograniczyć kamerę żeby nie mogła iść pod spód
- ustawianie begin_position i modelu dwukrtonie odwołuje się do api
- stawiać element w odległości min element - stała - wymyśleć coś lepszego po okręgu?
- zrobić focus podczas pisania że skróty klawiszowe nie działają
- przy zapisywaniu modeli zrobić że jak zapisuje wczytany to nadpisywać czyli przekazywać track_id
- zamienić że tracks jest w localStorage/sessionStorage
- przenieść bazę do mongo


do zrobienia później:
- przy odtwarzaniu modeli ma być numerek przy elemencie ile razy jeszcze go wykorzystać


Napotkange bugi:

- z czasem program działa coraz wolniej, wyciek pamięci?
- Elements lazy loading, we should stream elements when they are needed
wczytawanie określonej maksymalnej liczby elementów do wyboru z listy:
wczytanie modelu w momencie pierwszego dodawania do sceny w przypadku tworzenia modelu:
gdy odtwarzany model to wczytuje wszystkie elementy (może z ekranem loadingu), które mają zostać użyte.
- nie do końca działa ta kolizja, np. element ze spadkiem
- problem z kolizją po obrotach w okół osi x i y


06.10:

- ustawić gizmo konkretniej
- wyłączyć przemieszczenie kamery pod spód czy na pewno?
- wygenerować w blenderze preview aby przefiltrować elementy oraz zamienić thumbnail

12.10:

- złe wymiary w numpy array get connections
- naprawić przemieszczenia / łączenie
- sprawdzić kiedy token się usuwa przy przechodzeniu między stronami